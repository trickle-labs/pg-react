# v0.46.0 — Stack and stewardship contract qualification

> **Status:** Proposed; implementation blocked until the upstream start gate is met  
> **Predecessor:** `0.45.0`, or the actual qualified `0.45.x` patch if one intervenes  
> **Source milestone:** R0 — Stack and contract qualification  
> **Budget:** 30 person-days / six person-weeks  
> **Owners:** React runtime owner and adapter owner; MDM contract owner and upstream liaison supply dependencies; independent reviewer signs qualification

[Roadmap](../ROADMAP.md) · [Common release gates](../docs/planning/RELEASE-GATES.md) · [Evidence and sources](../docs/planning/EVIDENCE.md)

## 1. User-visible outcome

An operator can determine whether a specific PostgreSQL/pg_trickle/pg-react/MDM installation is eligible for the optional stewardship adapter, see a precise blocking reason when it is not, and reproduce the qualification result from pinned artifacts. The MDM-owned `MDM-STEWARDSHIP/1` interface is agreed and testable. No live routing, deadline-setting or escalation policy is enabled in this release.

R0 is not complete merely because a version check accepts pg_trickle 0.98.0. That release advertises disabled experimental Graph V1 and Delta V1; the React manifest also records a PostgreSQL image-identity discrepancy. These are the inspected starting facts, not defects this plan assumes have already been fixed. [S0 §2; S1–S5]

## 2. Entry gates and dependencies

Before implementation begins, the agreed upstream gate must pass on a released Graph V1-capable build, preserving the source proposal's start policy. Record public capability negotiation and independent conformance results, not just the version. The current planned upstream `0.104.0` milestone is a tracking reference, not an automatic approval.

The MDM owner must make the normative companion contract available and nominate a real artifact with review publication and an authorized policy projection for joint tests. Contract review is budgeted below; designing or implementing MDM's resolver, stewardship engine or public API is not. Missing companion text remains a blocker rather than an invitation to invent signatures.

Specification and test design may be prepared earlier. Earlier fixture implementation requires an explicit amendment to the start policy. If dependencies are unavailable, retain this plan as blocked and do not mark R0 delivered. [S0 §§2–3; S6]

## 3. Scope and explicit exclusions

**In scope:** exact-stack identity, an isolated integration environment, a thin public-contract capability adapter, privilege validation, approved contract mapping and shared test fixtures, fresh/upgrade/restart qualification, and installation without a mandatory MDM dependency.

**Out of scope:** replacing React's explicit coordinator with Graph V1; executing MDM graph refresh from React; Delta V1 consumers; WAL CDC; scheduler enablement; live policy consequences; general RLS; multiple administrative scopes; changes to MDM's identity or human-approval model.

The selected stack can contain an MDM-owned, qualified Graph V1 path. React continues to use its existing coordinator and to consume committed local policy tables. Preserve trigger capture, scheduler-off behavior and the existing differential-refresh safeguard unless an independently tested compatibility decision changes them. [S0; S2–S5]

## 4. Implementation design

### 4.1 Stack identity and admission

Capture effective database version, extension versions, artifact digests, platform, isolation, CDC and scheduler configuration. Resolve the reported PostgreSQL 18.3/18.4 discrepancy by obtaining and qualifying the intended artifact, or by an explicit support-boundary decision followed by qualification. Editing the manifest alone is not resolution.

Keep capability discovery behind one adapter boundary. Recognize the approved major and tested minor range; distinguish missing, unsupported, disabled and qualified-enabled states. Do not call Graph operations when disabled. A known disabled Delta capability is normal because this client does not use it. Capability discovery may run, but an ineligible configuration must fail before policy installation or effect admission.

Persist only the configuration/contract identities needed to explain admission. Do not mirror pg_trickle private catalogs. Read-only diagnostics must remain useful when MDM is absent. A capability change after deployment must block new adapter effects and require requalification rather than silently changing execution mode.

### 4.2 MDM-owned contract agreement

Obtain the companion's normative section and record its revision/digest. Confirm the proposed public names from the source—`mdm_steward.policy_cases_v1`, `mdm_steward.submit_policy_intent(...)`, and `mdm_steward.policy_receipts_v1`—against actual installed signatures. The ellipsis is not an executable API definition.

The agreement must settle:

| Contract topic | Decision required for the client |
|---|---|
| Case identity | Non-null unique `case_key bigint`, review identity, occurrence/reopening identity, stability across publication and recovery |
| Action-driving changes | Which revision changes when route, due date, escalation eligibility or relevant evidence changes; audit-only changes must not manufacture actions |
| Concurrency | Expected tokens, stale detection, human override behavior and atomic validation at intent application |
| Automation binding | Scope, enabled state, policy digest, allowed actions/queues/deadline/level limits, replacement and pause linearization |
| Request deduplication | Encoding version, key/body equality, duplicate response, same-key/different-body rejection and retention horizon |
| Receipts | Durable public correlation and the distinction between control application, accepted pending publication and completed publication |
| Permissions | Read, deploy, submit and observe rights; runtime must not have human approval or identity-write authority |
| Recovery | Coherent restore, clone activation guard, retention horizon and behavior when required prior receipts are unavailable |

These are client acceptance questions, not a competing definition of MDM's schema. Any unsupported mandatory behavior blocks effects and returns to the MDM owner for resolution. [S0 §§3, 6–8]

### 4.3 Fixtures and package structure

Add proposed locations `integrations/pg-mdm/contract/`, `tests/integrations/pg-mdm/contract/`, and `showcase/mdm-stewardship/`. Keep a frozen, provenance-stamped contract copy or approved reference, catalog assertions and shared fixtures.

Fixture rows must include ordinary review cases, a protected manual assignment, tied/no routing candidates, recurrence, stale evidence, a disabled binding and deadline bounds. Put synthetic data in a dedicated test schema/database, not a counterfeit live MDM API namespace. Mark fixture runs as fixture-only. Positive joint qualification must exercise the actual MDM artifact.

## 5. Work breakdown: 30 person-days

| Task | Days | Deliverable |
|---|---:|---|
| W46.1 — Baseline and contract inventory | 4 | Signed dependency checklist; exact preceding release; missing-interface decisions assigned |
| W46.2 — Artifact identity and admission adapter | 5 | Effective stack manifest, capability classification, fail-closed diagnostics |
| W46.3 — Coordination/ownership conformance | 6 | Public-only integration harness; rollback, boundary and normal-role coverage |
| W46.4 — Shared contract and fixtures | 5 | Approved interface mapping, typed catalog assertions, representative golden fixtures |
| W46.5 — Installation and recovery qualification | 5 | Fresh, adjacent upgrade, restart and disabled-adapter evidence on real artifacts |
| W46.6 — Documentation and independent review | 3 | Support matrix, deployment prerequisites, qualification report and release notes |
| W46.7 — Contingency | 2 | Corrections arising from integrated testing |
| **Total** | **30** | **Six person-weeks** |

Sequence W46.1 before contract-dependent implementation; W46.2 before positive live tests; W46.3/W46.4 before W46.5. Review security and contract identity before merging adapter code. External implementation or open-ended upstream debugging is not covered by this budget.

## 6. Acceptance tests

| ID | Scenario | Exact acceptance condition |
|---|---|---|
| T46.01 | pg_trickle 0.98 disabled capabilities | Discovery reports ineligible Graph admission; no graph or intent call, catalog mutation or frontier advance occurs |
| T46.02 | Qualified enabled build | Approved major/minor/profile and exact artifact pass the independent public-contract suite |
| T46.03 | Missing/unknown capability or signature | Clear unsupported result before mutation; no private fallback |
| T46.04 | Runtime image mismatch | Qualification fails when manifest identity and effective PostgreSQL version disagree |
| T46.05 | High-change-rate coordinated refresh | Lifecycle/work behavior remains correct through the inherited FULL-fallback risk; any changed safeguard has explicit evidence |
| T46.06 | Core without MDM | Ordinary core installation and representative rules work with adapter absent |
| T46.07 | Unauthorized role/hostile search path | Access denied without leaked policy facts or action capability; intended normal roles pass |
| T46.08 | Direct or transitive RLS source | Validation rejects deployment rather than bypassing RLS |
| T46.09 | MDM publication and rollback | Public policy rows/identities match the real MDM artifact; failed qualification transactions leave no partially published contract state |
| T46.10 | Adjacent upgrade and restart | Existing ordinary policies survive; adapter stays disabled; pinned contract and configuration remain explainable |

Use common gates C01–C05, C10 and C12. Record later delivery/time tests as owned by 0.48.0/0.49.0, not as passed by R0.

## 7. Migration, release and recovery

Append the actual predecessor-to-0.46.0 migration only when implementation changes require it. Package the optional adapter separately from the core dependency declaration. Its installation must default to no effects. Preserve published SQL and ordinary API behavior.

For a changed upstream runtime, stop/pause affected workers, retain a coherent backup with matching binaries and run the exact tested extension upgrade order. Return to the prior coherent artifact/database pair on failure; do not assume a reverse extension migration. Keep existing React coordination until a separately approved migration replaces it.

Publish the qualified environment, raw conformance evidence, unresolved optional capabilities and explicit adapter-disabled state. Update version metadata only when the actual release is implemented and qualified, not when this plan is adopted.

## 8. Definition of done and scope cut

R0 is done when IDENTITY, CONTRACT and required UPSTREAM/MDM-READ evidence pass; the real packaged stack passes its applicable common gates; and the next release can implement a client without guessing MDM SQL. A fixture-only harness is useful progress but cannot close this milestone.

Cut additional platform profiles, extra fixture domains and convenience diagnostics first. Do not cut effective-version checks, normal-role ownership, rollback or the normative contract. If enabling the necessary upstream behavior or building the MDM API is required, stop, record the external blocker, and re-estimate rather than absorbing it into six person-weeks.

**Next:** [v0.47.0 — Read-only policy package](v0.47.0.md).
