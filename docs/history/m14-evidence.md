# M14 evidence

`tests/m14.sh pg-react:v0.11.0` is the M14 repository gate. It freezes the M14 clean diagnostic envelope, five-role grant boundary, inferred positive dependency declaration, preview and atomic deployment, derived fact explanation, and populated `0.10.0 -> 0.11.0` upgrade.

The inherited M0–M13 fixtures remain the authoritative lifecycle, recovery, worker, deadline, negative, aggregate, performance, and external-effect evidence. M14 adds only public inspection and declaration translation.
