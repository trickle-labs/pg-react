# pg-react 0.44.0 final checklist

- [x] Current release metadata, extension defaults, and worker compatibility
      identify 0.44.0.
- [x] The 0.43.3 to 0.44.0 adjacent SQL and deterministic fresh-install
      artifact are assembled from explicit ordered inputs.
- [x] Target-scoped comparison provenance and one-pass rule-source validation
      have exact static and runtime contract checks.
- [x] Unmeasured scale, memory, WAL, retention, and recovery cases are called
      out as limits rather than release guarantees.
