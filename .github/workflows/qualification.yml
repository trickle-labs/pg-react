name: Canonical qualification

on:
  workflow_call:
    inputs:
      image:
        required: false
        type: string
        default: pg-react:v0.45.0-qualification

permissions:
  contents: read

jobs:
  qualification:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9
        with:
          toolchain: 1.89.0
          components: rustfmt
      - name: Create evidence directory
        run: mkdir -p v0.45.0-evidence
      - name: Rust format
        run: cargo fmt --check
      - name: Rust tests
        run: cargo test --no-default-features
      - name: Dependency audit
        run: cargo install cargo-audit --version 0.22.2 --locked && cargo audit
      - name: PostgreSQL 18 skeleton
        run: |
          docker run --rm \
            -v "$PWD:/work:ro" \
            -v /work/target \
            -w /work \
            ghcr.io/trickle-labs/pg-trickle/builder@sha256:8d0446c21ab3273b55c045a39c49120e9d7cde8e970954c3f81e7bee194fad95 \
            cargo check --features pg18
      - name: Build candidate
        run: |
          set +e
          docker build --tag "${{ inputs.image }}" . > v0.45.0-evidence/build.log 2>&1
          status=$?
          set -e
          exit "$status"
      - name: Current documentation audit
        run: bash tests/current-docs.sh
      - name: Workflow syntax audit
        run: bash tests/workflow-syntax.sh
      - name: API inventory audit
        env:
          API_INVENTORY_ARTIFACT: ${{ github.workspace }}/v0.45.0-evidence/installed-api-catalog.json
        run: bash tests/api-inventory.sh "${{ inputs.image }}"
      - name: Run complete candidate qualification
        env:
          V045_ARTIFACT_DIR: ${{ github.workspace }}/v0.45.0-evidence
        run: bash tests/qualification.sh complete "${{ inputs.image }}"
      - name: Archive candidate image
        run: |
          mkdir -p v0.45.0-evidence
          docker image inspect --format '{{.Id}}' "${{ inputs.image }}" > v0.45.0-evidence/candidate-image.txt
          docker save "${{ inputs.image }}" | gzip -n -9 > v0.45.0-evidence/candidate-image.tar.gz
          sha256sum v0.45.0-evidence/candidate-image.tar.gz > v0.45.0-evidence/candidate-image.SHA256SUMS
      - name: Write evidence manifest
        run: |
          set -eu
          evidence_dir=v0.45.0-evidence
          test -s "$evidence_dir/candidate-image.txt"
          test -s "$evidence_dir/candidate-image.tar.gz"
          test -s "$evidence_dir/candidate-image.SHA256SUMS"
          entries='[]'
          while IFS= read -r artifact; do
            test -f "$artifact"
            digest=$(sha256sum "$artifact" | awk '{print $1}')
            entries=$(jq --arg artifact "$artifact" --arg digest "$digest" \
              '. + [{milestone: "PGTRICKLE-098", mode: "executed", artifact: $artifact, sha256: $digest}]' <<< "$entries")
          done < <(find "$evidence_dir" -type f ! -name evidence-manifest.json | sort)
          jq -n \
            --arg commit "$GITHUB_SHA" \
            --arg run "$GITHUB_RUN_ID" \
            --argjson entries "$entries" \
            '{schema_version: 1, milestone: "PGTRICKLE-098", commit: $commit, workflow_run: $run, entries: $entries}' \
            > v0.45.0-evidence/evidence-manifest.json
      - name: Write failed evidence manifest
        if: ${{ failure() }}
        run: |
          jq -n \
            --arg commit "${GITHUB_SHA}" \
            --arg run "${GITHUB_RUN_ID}" \
            '{schema_version: 1, milestone: "PGTRICKLE-098", status: "failed",
              commit: $commit, workflow_run: $run,
              missing_steps: ["candidate qualification"]}' \
            > v0.45.0-evidence/evidence-manifest.json
      - name: Upload qualified candidate and evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: v0.45.0-qualified-candidate
          path: v0.45.0-evidence/
