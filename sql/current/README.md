# Current SQL

`assembly.txt` is the ordered input list for the current installation artifact.
The predecessor artifact is retained byte-for-byte; current authored changes
are isolated in `comparison.sql`. Published historical migration files remain
frozen.
