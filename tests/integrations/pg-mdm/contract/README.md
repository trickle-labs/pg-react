# Contract qualification seam

Run the canonical exact-output contract check from the repository root:

```text
python3 tests/mdm_stewardship_contract.py
```

This proves the approved contract and vectors only. It does not claim live
pg-mdm M1 qualification; that gate remains blocked until the installed MDM
artifact exposes the authorized policy-case projection and occurrence mapping.
