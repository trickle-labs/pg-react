# pg-react v0.48.0 recorded joint checks

The recorded joint checks passed on 2026-09-24 in the disposable `pg-react-mdm-m2-clean` container. They used an earlier, uncommitted pg-react source snapshot. The live worker and security checks were not rerun against this candidate. They do not qualify its SQL changes.

## Source and package identity

| Item | Identity |
| --- | --- |
| pg-react base commit | `991bf9fd1d3de6102077afaeb1c9f64a11c1681c`; the qualification changes were in the uncommitted worktree |
| pg-mdm commit | `5366a820e0db6a7d65567a2a6e38e7276ce8e97f` |
| pg-react extension | `0.46.0` |
| pg-mdm extension | `0.14.0` |
| pg_trickle extension | `0.108.0` |
| pg-mdm base image | `pg_mdm@sha256:2b3da6d0fbbad930bab45aca7b12d691e9e8801548f06135c0877ed657ee7e9d` |
| Joint runtime image used for SQL qualification | `pg-react-mdm@sha256:72f4bbb5d3f1bd55512d4ca937cf15932e23fb0a83025289461bd6ed0bfa0705` |
| Joint image rebuilt with the restore helper file | `pg-react-mdm@sha256:dd194d0e4739f7e4c148a51b4a8536bd9271fc57806087d433ba9f4d32747fef` |
| M2 policy package | `v0.48-live-policy`, digest `b60bff5c255647790476fe398e329da39ff48fa07dce32aa9842c0ae5cbd7db6` |
| pg-mdm shared library | `64d556d6afdd209f24115ada3eab36bff2fa2f5b0ef127ca2f37b7511df9488d` |
| pg-mdm 0.14.0 install SQL | `b65e9bcb9b9f2b4ed35fd9477b6b948ced72ab0e9126f05f347ee92de53f92d4` |
| pg-mdm configure helper SQL | `f62b2056b1faad1f87049e77cc61d7ceda02218292cb6bc8b2dffb9e0636ad37` |

The restore run used the helper SQL copied from the pg-mdm image. The rebuilt joint image contains the same file at `tests/integrations/pg-mdm/pg-mdm-configure-helper.sql`.

## Roles exercised

The setup connection used `postgres`. The worker test connected as `mdm_m2_runner` and ran with `SET ROLE pgreact_mdm_worker`. The worker was `NOLOGIN`, `NOSUPERUSER`, `NOBYPASSRLS`, and `NOINHERIT`. The entity roles were `mdm_legacy_administrator` and `mdm_administrator`. The MDM helper role was `mdm_helper_owner`.

## Commands and results

The SQL tests ran against database `foundation` on port `55432`.

```sh
rtk psql -h 127.0.0.1 -p 55432 -U postgres -d foundation -v ON_ERROR_STOP=1 -f tests/integrations/pg-mdm/v0.48-codec.sql
rtk psql -h 127.0.0.1 -p 55432 -U postgres -d foundation -v ON_ERROR_STOP=1 -f tests/integrations/pg-mdm/v0.47-live-setup.sql
rtk psql -h 127.0.0.1 -p 55432 -U postgres -d foundation -v ON_ERROR_STOP=1 -f tests/integrations/pg-mdm/v0.47-live.sql
rtk psql -h 127.0.0.1 -p 55432 -U postgres -d foundation -v ON_ERROR_STOP=1 -f tests/integrations/pg-mdm/v0.48-live.sql
rtk psql -h 127.0.0.1 -p 55432 -U postgres -d foundation -v ON_ERROR_STOP=1 -f tests/integrations/pg-mdm/v0.48-security.sql
PGHOST=127.0.0.1 PGPORT=55432 PGUSER=postgres MDM_HELPER_CONFIG=/private/tmp/mdm-m2-configure_helper.sql rtk sh tests/integrations/pg-mdm/v0.48-restore.sh
```

The commands returned zero. Their summary output was:

```text
v0.48 canonical digest, request-key, body, and retry checks: PASS
v0.47 live MDM policy package passed
v0.48 actual worker actions, limits, receipts, and observation stability passed
v0.48 actual worker privileges and retry passed
v0.48 logical restore and OID reconciliation passed
```

The restore check confirmed durable request, attempt, and receipt correlation. It rejected work before entity rebind and runtime reconciliation. The MDM runtime row was absent in the restored database. React's saved runtime row kept the old database and worker OIDs. Reconciliation then recorded the current identities.

This M2 check does not recompile MDM graphs. The upstream pg-mdm restore test covers that path. A joint-image attempt to recompile hit a reset `pgtrickle.pgt_stream_tables_pgt_id_seq` and a duplicate `pgt_id`.

The updated image built with:

```sh
rtk docker build --quiet -f tests/integrations/pg-mdm/Dockerfile.v0.48 -t pg-react-mdm:0.48-m2 .
```

The build returned `sha256:dd194d0e4739f7e4c148a51b4a8536bd9271fc57806087d433ba9f4d32747fef`. A container check confirmed that the packaged configure helper file exists and matches the file used by the restore command.

## Exact-source rerun on pg-mdm 0.14.0

The 2026-09-24 run used pg-react HEAD `b87ed0eccb32329e7142987721117826d2f50975` plus the four checked-in SQL changes listed in the manifest. It used the local `pg_mdm:0.14.0-e2e` image, digest `sha256:45b99615f7d47706dd2a0c512caa5e4ddba23b0fcb060ca65afb8462a2bf7221`, with source revision label `4889ac41485ac1282aa90b261487ba5315501d10`.

The joint image was built from that worktree:

```sh
rtk docker build --quiet -f tests/integrations/pg-mdm/Dockerfile.v0.48 -t pg-react-mdm:v0.48.0-qual-final .
```

It returned `sha256:02eb700b0fdf55d35b3d7f39303fef4e3f76568d8add6e441921572107d141c9`. The clean runner was `pg-react-v048-qual6`, mapped to port `55443`. It used PostgreSQL 18.4, pg-react 0.46.0, pg-mdm 0.14.0, and pg-trickle 0.108.0. The runner bootstrapped `/tests/e2e.sql` and `/tests/e2e_policy.sql`; the second file supplies `policy_qualification`.

The checked-in SQL checks passed:

```text
v0.47 normal-role comparison passed
v0.47 read-only policy package passed
v0.47 typed package identity passed
v0.47 live MDM policy package passed
v0.48 canonical digest, request-key, body, and retry checks: PASS
v0.48 actual worker actions, limits, receipts, and observation stability passed
v0.48 actual worker privileges and retry passed
v0.48 logical restore and OID reconciliation passed
```

The upstream fixture now has one open case without an authorized opening time, so `v0.47-live.sql` asserts one such finding. The live worker check compares the complete applied request and receipt vector. Its observation-only publication check confirms that the publication adds no worker intent attempts and changes no action or decision state. The restore script now removes grants that `mdm_helper_owner` made before it recreates the worker role. The restored `policy_qualification` binding must retain exactly two correlated receipts, one for the due action and one for escalation.

This run supports T48.01, T48.02, T48.03, T48.11, and T48.12. T48.04-T48.10 remain open. The tests do not cover failure after MDM submission, ambiguous-commit disconnect and replay, concurrent human edits, pause or replacement races, fresh reevaluation after stale work, denied or unknown outcomes, or pending-publication separation. The retention horizon and MDM owner approval are also unrecorded. The candidate remains blocked on those gates and approvals.

## Repaired exact-snapshot qualification (2026-09-25)

The repaired run used PR #6 head `d73f03ea18d06d10891ea352d743eedbb0e6bf2f` plus the seven-file overlay with snapshot SHA-256 `9d38fc79c7278a94e3c560261e57f48fa7d8f52066c26aff92ad0a06a657ec21`. The recoverable source archive is `run-2026-09-25/candidate-snapshot.tar` (SHA-256 `762389923899b2934110101a6da6841e536bdbe19b3597fed05c91814336e95a`). The repaired image is `sha256:44c0242a6b00e0934092ff2bda93b289a836b8d1712f4d149b4008b7bfcbfaa7` with PostgreSQL 18.4, pg-react 0.46.0, pg-mdm 0.14.0, and pg-trickle 0.108.0.

The run passed the populated adjacent upgrade, live worker, security, restore/OID reconciliation, missing-MDM admission, request-codec, and v0.47 live prerequisite lanes. The adjacent-upgrade fixture preserved an old-schema populated hold and its explicit migrated `action_revision`; all recorded artifact and source hashes verify against the manifest.

The `9d38fc79…` candidate is `candidate_owner_signed`. Contract v4 records the owner sign-off for that snapshot and waives independent review for this solo-maintainer release; those original qualification artifacts remain bound to that fingerprint.

## Admission workflow repair

The v0.48 workflow now runs the missing-MDM admission script as the PostgreSQL superuser, matching the script's `createdb`/`dropdb` requirements. On image `sha256:44c0242a6b00e0934092ff2bda93b289a836b8d1712f4d149b4008b7bfcbfaa7`, `rtk docker exec --user postgres pg-react-pr6-ci-fix sh /work/tests/integrations/pg-mdm/v0.48-admission.sh` passed with `v0.48 missing-MDM admission rejected before effects: PASS`; see `run-2026-09-25/v048-admission-recheck.log`.

The workflow edit changes the current seven-file source snapshot to `176b4654bd47803f396cdd509140de342c379b5ba5a6bf33d574db7878ba9bf1`, archived at `run-2026-09-25/candidate-snapshot-workflow-fix.tar` (SHA-256 `707fd54b78c0782d0c70ec396447c0d4e2a0fe296415c722254af2624c8effc3`). The full T48.01–T48.12 qualification artifacts above remain bound to the prior `9d38fc79c7278a94e3c560261e57f48fa7d8f52066c26aff92ad0a06a657ec21` snapshot; product SQL and test sources are unchanged. Issue #7 contract v5 records owner sign-off for the current snapshot against base `678e92d6a0f4b23d5f0d36c0829ff9b898ad51e5`. Independent review is waived for this solo-maintainer release. Current-head qualification runs remain pending and must pass before proof or merge readiness.
